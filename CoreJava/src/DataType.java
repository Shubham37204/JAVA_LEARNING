void main() {
    //declaration of variable
    //DataType variableName assignmentOperator Value;
    int num = 4;
    String name = "Shubham";
    boolean b=true;
    float f=5.8f;

    char c='k';
    //char(2 byte) follow Unicode so it has bigger range
    //char can store any letter,numerbs,symbol, etc

    double d = 5.6;
    //in java Double is default for decimal value and
    // hence to use float as decimal value use this as notation valuef

    System.out.print(num +" "+ name+" " +b+" " +f+" " +c);
}
